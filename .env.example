# Required for /admin and /api/admin/tokens. Use a long, unique secret.
ADMIN_SECRET=

# Required in production for persistent private-access tokens.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Used when creating links outside the browser UI.
SITE_URL=https://www.cmlearn.tech
